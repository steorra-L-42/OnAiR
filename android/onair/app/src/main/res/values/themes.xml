<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 일반 앱 테마 -->
    <style name="Theme.OnAiR" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- 일반 테마 속성들 필요시 넣으면 됨 -->
    </style>
    <!-- 스플래시 테마 -->
    <style name="Theme.OnAiR.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.OnAiR</item>
        <item name="android:windowBackground">@color/splash_background</item> <!-- Android 11 미만용 -->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
